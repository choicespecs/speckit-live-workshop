node_modules/
.claude/settings.local.json
.DS_Store

# Presentation: only the finished deck is a repo deliverable. Everything
# else (build scripts, source markdown, thumbnails, node_modules) stays
# local only, needed just to regenerate the deck.
presentation/*
!presentation/SpecDrivenDevelopment.pptx
